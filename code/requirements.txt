# Runnable reference-code dependencies (heavier than the docs toolchain).
# Install with:  pip install -r code/requirements.txt
#
# torch: CPU wheels are fine for the numpy/PyTorch reference tests.
#   For GPU, install the build matching your platform:
#     - NVIDIA (CUDA):  the default pytorch.org CUDA wheels
#     - AMD (ROCm):     the ROCm wheels (e.g. torch ... +rocmX.Y)
# triton ships with recent GPU torch builds (CUDA and ROCm). The Triton tests
# skip automatically when no GPU / triton is present.
numpy>=1.24
torch>=2.2
pytest>=7.0
# triton            # provided by GPU torch builds; uncomment to pin explicitly
